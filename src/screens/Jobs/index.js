import React from 'react';
import { View, Text } from 'react-native';

const Jobs = () => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text style={{ fontSize: 20 }}>Jobs Screen</Text>
        </View>
    );
}
export default Jobs;